{
  "name": "training-learning-rails-ci-public",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:pr265": "node cases/pr-265/run-validation-v2.mjs",
    "security:gate": "node scripts/public-boundary-gate.mjs",
    "check": "npm run security:gate && npm test && npm run test:pr265"
  }
}
