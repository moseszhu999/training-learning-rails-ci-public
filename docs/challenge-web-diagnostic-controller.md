# Challenge Web sanitized diagnostics

This controller extension reports only TypeScript file paths, line numbers, compiler codes, and a fixed build substage label. It never publishes compiler messages, private source, raw logs, artifacts, secrets, deployment output, or database content.
