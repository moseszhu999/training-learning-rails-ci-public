name: Recipe Runtime PR325 fresh database replay

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  parser-smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm workflow parser
        run: echo 'RECIPE_DB_WORKFLOW_PARSER PASS'
